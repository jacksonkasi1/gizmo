import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SearchFlatlist = () => {
  return (
    <View style={styles.container}>
      <Text>SearchFlatlist</Text>
    </View>
  )
}

export default SearchFlatlist

const styles = StyleSheet.create({
  container:{
    flex:1,
    backgroundColor:'#fff'
  }
})