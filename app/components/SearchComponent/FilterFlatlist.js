import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const FilterFlatlist = () => {
  return (
    <View>
      <Text>FilterFlatlist</Text>
    </View>
  )
}

export default FilterFlatlist

const styles = StyleSheet.create({})